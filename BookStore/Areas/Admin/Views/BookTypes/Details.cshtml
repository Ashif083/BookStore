@model BookStore.Models.BookTypes

@{
    ViewData["Title"] = "Details";
}

<h1 class="text-info">Genre Details</h1>

<div>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.BookType)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.BookType)
        </dd>
    </dl>
</div>
<div>
   <a asp-controller="BookTypes" asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Edit</a>
    <a asp-action="Index" class="btn btn-success">Back to List</a>
</div>
